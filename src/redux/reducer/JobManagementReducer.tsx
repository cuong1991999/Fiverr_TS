import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const JobManagementReducer = createSlice({
  name: "JobManagementReducer",
  initialState,
  reducers: {},
});

export const {} = JobManagementReducer.actions;

export default JobManagementReducer.reducer;
